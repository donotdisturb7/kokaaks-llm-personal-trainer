# ==============================================================================
# CONFIGURATION DOCKER COMPOSE
# ==============================================================================
# Copier ce fichier vers .env: cp .env.example .env
# Puis éditer .env avec vos valeurs

# ==============================================================================
# LLM PROVIDER
# ==============================================================================

# Provider: "groq" (recommandé) ou "ollama"
LLM_PROVIDER=groq

# Clé API Groq (obligatoire si LLM_PROVIDER=groq)
# Obtenir une clé gratuite sur: https://console.groq.com
GROQ_API_KEY=your_groq_api_key_here

# URL Ollama (si LLM_PROVIDER=ollama)
# Pour Ollama sur votre machine hôte depuis Docker:
OLLAMA_BASE_URL=http://host.docker.internal:11434

# ==============================================================================
# KOVAAK'S
# ==============================================================================

# Votre username KovaaK's (optionnel)
KOVAAKS_USERNAME=

# ==============================================================================
# NOTES
# ==============================================================================
# 
# Les autres variables (DATABASE_URL, REDIS_URL, KOVAAKS_PROXY_URL) sont 
# configurées automatiquement dans docker-compose.yml car elles utilisent 
# les noms de services Docker (postgres, redis, kovaaks-proxy).
#
# Pour le développement local sans Docker, voir backend/env.example
